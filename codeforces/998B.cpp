#include <bits/stdc++.h>
using namespace std;

void bruteforce()
{
	// 1 2 5 10
	// dp[1, 4] => value of cut satisfies condition
	// dp[1, 2] + dp[3, 4]
	// dp[1, 2] + dp[
}

void solve()
{
	int n, B;
	cin >> n >> B;

	vector<int> a(n);
	for (auto &x: a)
		cin >> x;

	dp[i][j];
	
}

int main()
{
	solve();
	return EXIT_SUCCESS;
}
